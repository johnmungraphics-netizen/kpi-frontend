import { combineReducers } from '@reduxjs/toolkit';

// Empty for now - add feature slices here when implementing Redux
const rootReducer = combineReducers({
  // Future slices will go here
  _placeholder: (state = {}) => state, // Prevents empty reducer error
});

export default rootReducer;
